[tool.pyright]
pythonVersion = "3.12"
pythonPlatform = "Darwin"
typeCheckingMode = "basic"

# Add src to the search path
extraPaths = ["src"]

[tool.pytest.ini_options]
pythonpath = [
  "."
]
testpaths = [
    "src/engine/tests"
]
asyncio_mode = "auto"

[tool.pyre]
search_path = ["src"]
